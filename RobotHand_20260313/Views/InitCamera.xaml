<Window x:Class="RobotHand_20260313.Views.InitCamera"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RobotHand_20260313.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
         Style="{StaticResource MaterialDesignWindow}"
        Title="图像标定窗口" Height="600" Width="800"
         Loaded="InitCamera_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="260"/>
        </Grid.ColumnDefinitions>
        <materialDesign:Snackbar x:Name="Snackbar" Panel.ZIndex="1" MessageQueue="{materialDesign:MessageQueue}"   Background="#4CAF50"
                         Foreground="White" VerticalAlignment="Center"/>
        <Image Name="ImageView" Margin="10"
        Stretch="Uniform"
        MouseLeftButtonDown="ImageView_MouseLeftButtonDown"/>
        <DockPanel Grid.Column="1"  Margin=" 10,30">
            <TextBlock MinHeight="200" Grid.Column="1"  Margin="10" DockPanel.Dock="Top"  TextWrapping="Wrap"   FontSize="14"    LineHeight="25" 
           LineStackingStrategy="BlockLineHeight">
    1. 初始化标定窗口的作用是当摄像头的工作环境、位置发生人为的移动或者改变时，重新定位所需要做的操作。
    <LineBreak/>
    <LineBreak/>  <!-- 插入一个空行，增加段落间距 -->
    2. 操作包含两个部分，第一步是将与设备附带的定位纸张完整呈现在摄像头画面中
    <LineBreak/>
    <LineBreak/>
    3. 第二步是点击“更新图像”按钮进行拍照，在抓拍的照片中依次点击9个点，最后点击第10个点。点击“保存标定”按钮，就完成了整个操作。
</TextBlock>
          
            <StackPanel DockPanel.Dock="Bottom"  >
                <Button Content="保存标定" Click="SaveCalibration_Click" Margin="10"/>
                <!-- 按钮加载图像 -->
                <Button Content="更新图像" Click="LoadImageCalibration_Click" Margin="10"/>
            </StackPanel>
        </DockPanel>
       
    </Grid>
</Window>
